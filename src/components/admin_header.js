import React, { Component } from "react";

class AdminHeader extends Component {
    render() {
        return (
            <div>Header {React.version}</div>
        );
    }
}

export default AdminHeader;